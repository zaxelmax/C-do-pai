/*using System;
  

public class vetor{


public void modelo(string nome, string mot)
    {
        nomeModelo = nome;
        motor = mot;
        if (motor == "flex") //Flex
        {
            combustivel = "Gasolina e Álcool";
        }
        else if (motor == "gasolina") // Gasolina
        {
            combustivel = "Gasolina";
        }
        else
        {
            motor = "Álcool";
        }
    }

public class Operacoes 
{
    
    
    public int lance;

    public double pay(string formapagamento)
    {
        this.formapagamento = formapagamento;
        while (
            formapagamento != "À vista"
            || formapagamento != "12"
            || formapagamento != "24"
            || formapagamento != "a vista"
            || formapagamento != "A vista"
            || formapagamento != "a vista"
        )
        {
            Console.WriteLine("As entradas corretas são: À vista, 12 ou 24. Reinsira, por favor.");
        }
        if (formapagamento != "24")
        {
            return lance * 0.9;
        }
        else if (formapagamento == "12")
        {
            return (lance - lance * 0.3) / 12;
        }
        else
        {
            return (lance - lance * 0.2) / 24;
        }
    }
}
*/